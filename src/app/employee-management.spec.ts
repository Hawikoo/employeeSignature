import { EmployeeManagement } from './employee-management';

describe('EmployeeManagement', () => {
  it('should create an instance', () => {
    expect(new EmployeeManagement()).toBeTruthy();
  });
});
